# -----------------------------------------------------------------
# KONFIGURASI WA-SERVICE (NODE.JS)
# -----------------------------------------------------------------

# Port di mana service ini akan berjalan (diakses di http://localhost:3100)
PORT=3100

# --- Keamanan API (WAJIB) ---
# Kunci ini harus SAMA PERSIS dengan 'WA_SERVICE_API_KEY' di .env Laravel Anda.
API_KEY=ROMS_Proyek3_Polban_k3Y!_sEcuRe987

# URL API Laravel (ROMS) tempat kita akan mengirim pesan baru
ROM_WEBHOOK_URL=http://host.docker.internal:8000/api/v1/webhook/whatsapp-inbound

# --- Konfigurasi Webhook (WAJIB) ---
# URL tempat service ini akan mengirim pesan masuk (ke API Laravel Anda)
# Kita gunakan 'host.docker.internal' agar kontainer Docker bisa "melihat"
# server Laravel Anda (php artisan serve) yang berjalan di 'localhost:8000'
AUTOMATION_WEBHOOK_URL=

# (Biarkan kosong jika tidak ada header keamanan tambahan)
AUTOMATION_WEBHOOK_HEADER_KEY=
AUTOMATION_WEBHOOK_HEADER_VALUE=

# --- Konfigurasi Klien WA ---
# Otomatis memulai klien dengan ID 'mywa1' saat startup
WA_CLIENT_IDS=official_business

# --- Konfigurasi Database & Sesi (Untuk Docker) ---
# Variabel-variabel ini sudah diatur di docker-compose.yml,
# tapi kita tetap definisikan di sini untuk kelengkapan.
REDIS_URL=redis://redis:6379
SQLITE_PATH=/data/db.sqlite
WWEBJS_DATA_PATH=/data/sessions
SESSIONS_DIR=/data/sessions
PUPPETEAR_EXECUTABLE_PATH=/usr/bin/chromium

# --- Konfigurasi Scheduler (Biarkan default) ---
SCHEDULER_BASE_DELAY_MS=500
SCHEDULER_MAX_ATTEMPTS=8

# --- Konfigurasi Puppeteer (Biarkan default) ---
PUPPETEER_LAUNCH_TIMEOUT_MS=60000